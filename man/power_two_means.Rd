% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous-two-means.R
\name{power_two_means}
\alias{power_two_means}
\title{Power for the difference of two independent means}
\usage{
power_two_means(
  n1,
  n2 = n1,
  effect,
  sd,
  alpha = 0.025,
  hypothesis = c("superiority", "noninferiority", "superiority_margin", "equivalence"),
  margin = NULL,
  sides = if (hypothesis == "superiority") 2L else 1L,
  test = c("t", "z")
)
}
\arguments{
\item{n1}{Sample size in group 1.}

\item{n2}{Sample size in group 2. Defaults to \code{n1} (equal allocation).}

\item{effect}{True difference in means, \eqn{\mu_1 - \mu_2}.}

\item{sd}{Common standard deviation of the endpoint.}

\item{alpha}{Significance level (one-sided for margin-based hypotheses).}

\item{hypothesis}{One of \code{"superiority"}, \code{"noninferiority"},
\code{"superiority_margin"}, or \code{"equivalence"}.}

\item{margin}{Positive margin, required for all hypotheses except
\code{"superiority"}.}

\item{sides}{1 or 2. Used only for \code{hypothesis = "superiority"}.}

\item{test}{\code{"t"} for the noncentral t distribution (default) or
\code{"z"} for the normal approximation.}
}
\value{
A single numeric value: the power at the specified sample sizes.
}
\description{
Computes the power of a two-sample test comparing two independent means
at a fixed sample size. Use \code{\link{ss_two_means}} to find the sample
size that achieves a target power.
}
\examples{
# Power at n = 86 per group (should be just over 0.90)
power_two_means(n1 = 86, effect = 5, sd = 10, alpha = 0.05, sides = 2)

# Power curve: how does power grow with n?
n_seq <- seq(20, 150, by = 10)
pwr   <- sapply(n_seq, function(n)
  power_two_means(n1 = n, effect = 5, sd = 10, alpha = 0.05, sides = 2))
plot(n_seq, pwr, type = "l", xlab = "n per group", ylab = "Power")
abline(h = 0.90, lty = 2)

}
\references{
Julious, S.A. (2004). Sample sizes for clinical trials with Normal
data. \emph{Statistics in Medicine}, 23(12), 1921--1986.

Chow, S.C., Shao, J., Wang, H. (2008). \emph{Sample Size Calculations
in Clinical Research}, 2nd ed. Chapman & Hall/CRC. Chapter 3.
}
